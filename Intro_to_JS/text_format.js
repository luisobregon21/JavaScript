#!/usr/bin/env node
// prints using text formatting
const width = 10.101;
const length = 3.843;
// the `` quotes, allows for formatting
console.log(`The rectangle is ${length} cm long and ${width} cm wide.`);
